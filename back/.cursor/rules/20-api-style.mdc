---
alwaysApply: true
---
- REST 경로: /api/v1 기준, 리소스는 복수형(/members, /posts).
- 응답은 표준 포맷을 사용한다: { success, data, error } (프로젝트에 맞춰 통일)
- 검증은 jakarta validation 사용: @Valid + DTO에 어노테이션.
- 페이지네이션은 Spring Data Pageable 사용(가능하면).